# devchan

> An image board for programmers.

## Installation

    $ cd devchan
    $ npm install
    $ node seeds
    $ node server

## Development

    $ npm install nodemon -g
    $ nodemon server.js --watch app --watch node_modules

    $ node shell
